- name: Run AI Writer
        env:
          # This maps the GitHub Secret to the Python Environment Variable
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENROUTER_API_KEY }} 
        run: python auto_blogger.py
